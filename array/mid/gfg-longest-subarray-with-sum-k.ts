/**
 * Problem: Longest Subarray with Sum K
 * Difficulty: Medium
 *
 * Description:
 * Given an array of integers and a target sum k, find the length of the longest subarray whose elements sum to k.
 * Uses a prefix sum with a hash map to track the earliest index at which each prefix sum was seen.
 * This is a GFG problem on subarrays with a given sum.
 *
 * Time Complexity: O(n)
 * Space Complexity: O(n)
 */
